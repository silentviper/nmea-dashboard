/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/nmea-dashboard',
    reactStrictMode: true,
    sassOptions: {
    },
};

export default nextConfig;
